services:
  caddy:
    image: caddy:alpine
    ports:
      - "${HTTP_PORT}:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./mta-sts.txt:/srv/.well-known/mta-sts.txt:ro
    restart: unless-stopped
